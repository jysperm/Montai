diff --git a/node_modules/@mariozechner/pi-ai/dist/models.generated.js b/node_modules/@mariozechner/pi-ai/dist/models.generated.js
index 927aa8a..8463f24 100644
--- a/node_modules/@mariozechner/pi-ai/dist/models.generated.js
+++ b/node_modules/@mariozechner/pi-ai/dist/models.generated.js
@@ -3827,6 +3827,23 @@ export const MODELS = {
             contextWindow: 1048576,
             maxTokens: 65536,
         },
+        "gemini-3.5-flash": {
+            id: "gemini-3.5-flash",
+            name: "Gemini 3.5 Flash",
+            api: "google-generative-ai",
+            provider: "google",
+            baseUrl: "https://generativelanguage.googleapis.com/v1beta",
+            reasoning: true,
+            input: ["text", "image"],
+            cost: {
+                input: 1.5,
+                output: 9,
+                cacheRead: 0.15,
+                cacheWrite: 0,
+            },
+            contextWindow: 1048576,
+            maxTokens: 65536,
+        },
         "gemini-3-pro-preview": {
             id: "gemini-3-pro-preview",
             name: "Gemini 3 Pro Preview",
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js b/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js
index 1062755..540a3a9 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js
@@ -72,6 +72,8 @@ const fromClaudeCodeName = (name, tools) => {
  * Convert content blocks to Anthropic API format
  */
 function convertContentBlocks(content) {
+    // Filter out file content (Google-specific)
+    content = content.filter((c) => c.type !== "file");
     // If only text blocks, return as concatenated string for simplicity
     const hasImages = content.some((c) => c.type === "image");
     if (!hasImages) {
@@ -739,7 +741,7 @@ function convertMessages(messages, model, isOAuthToken, cacheControl) {
                 }
             }
             else {
-                const blocks = msg.content.map((item) => {
+                const blocks = msg.content.filter((item) => item.type !== "file").map((item) => {
                     if (item.type === "text") {
                         return {
                             type: "text",
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/google-shared.js b/node_modules/@mariozechner/pi-ai/dist/providers/google-shared.js
index 1bc51fa..a7eb28d 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/google-shared.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/google-shared.js
@@ -98,6 +98,22 @@ export function convertMessages(model, context) {
                     if (item.type === "text") {
                         return { text: sanitizeSurrogates(item.text) };
                     }
+                    else if (item.type === "file") {
+                        const part = {
+                            fileData: {
+                                fileUri: item.uri,
+                                mimeType: item.mimeType,
+                            },
+                        };
+                        if (item.videoMetadata) {
+                            part.videoMetadata = {
+                                startOffset: item.videoMetadata.startOffset,
+                                endOffset: item.videoMetadata.endOffset,
+                                ...(item.videoMetadata.fps !== undefined ? { fps: item.videoMetadata.fps } : {}),
+                            };
+                        }
+                        return part;
+                    }
                     else {
                         return {
                             inlineData: {
@@ -176,12 +192,13 @@ export function convertMessages(model, context) {
             });
         }
         else if (msg.role === "toolResult") {
-            // Extract text and image content
+            // Extract text, image, and file content
             const textContent = msg.content.filter((c) => c.type === "text");
             const textResult = textContent.map((c) => c.text).join("\n");
             const imageContent = model.input.includes("image")
                 ? msg.content.filter((c) => c.type === "image")
                 : [];
+            const fileContent = msg.content.filter((c) => c.type === "file");
             const hasText = textResult.length > 0;
             const hasImages = imageContent.length > 0;
             // Gemini 3+ models support multimodal function responses with images nested inside
@@ -224,6 +241,35 @@ export function convertMessages(model, context) {
                     parts: [{ text: "Tool result image:" }, ...imageParts],
                 });
             }
+            // Add file references (e.g. Gemini File API videos) after function response
+            if (fileContent.length > 0) {
+                const fileParts = fileContent.map((fileBlock) => {
+                    const part = {
+                        fileData: {
+                            fileUri: fileBlock.uri,
+                            mimeType: fileBlock.mimeType,
+                        },
+                    };
+                    if (fileBlock.videoMetadata) {
+                        part.videoMetadata = {
+                            startOffset: fileBlock.videoMetadata.startOffset,
+                            endOffset: fileBlock.videoMetadata.endOffset,
+                            ...(fileBlock.videoMetadata.fps !== undefined ? { fps: fileBlock.videoMetadata.fps } : {}),
+                        };
+                    }
+                    return part;
+                });
+                const lastContent = contents[contents.length - 1];
+                if (lastContent?.role === "user") {
+                    lastContent.parts.push(...fileParts);
+                }
+                else {
+                    contents.push({
+                        role: "user",
+                        parts: fileParts,
+                    });
+                }
+            }
         }
     }
     return contents;
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/google.js b/node_modules/@mariozechner/pi-ai/dist/providers/google.js
index ffbd238..2ab4709 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/google.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/google.js
@@ -152,6 +152,12 @@ export const streamGoogle = (model, context, options) => {
                     if (output.content.some((b) => b.type === "toolCall")) {
                         output.stopReason = "toolUse";
                     }
+                    // Preserve the raw Gemini finishReason so the generic "error"
+                    // stopReason can be reported with its real cause (e.g.
+                    // MALFORMED_FUNCTION_CALL, UNEXPECTED_TOOL_CALL, SAFETY).
+                    if (output.stopReason === "error") {
+                        output.errorFinishReason = candidate.finishReason;
+                    }
                 }
                 if (chunk.usageMetadata) {
                     output.usage = {
@@ -193,7 +199,9 @@ export const streamGoogle = (model, context, options) => {
                 throw new Error("Request was aborted");
             }
             if (output.stopReason === "aborted" || output.stopReason === "error") {
-                throw new Error("An unknown error occurred");
+                throw new Error(output.errorFinishReason
+                    ? `Gemini stopped with finishReason: ${output.errorFinishReason}`
+                    : "An unknown error occurred");
             }
             stream.push({ type: "done", reason: output.stopReason, message: output });
             stream.end();
@@ -218,7 +226,12 @@ export const streamSimpleGoogle = (model, context, options) => {
     if (!apiKey) {
         throw new Error(`No API key for provider: ${model.provider}`);
     }
-    const base = buildBaseOptions(model, options, apiKey);
+    const base = {
+        ...buildBaseOptions(model, options, apiKey),
+        // streamSimple accepts provider-specific options at runtime. Preserve
+        // Google's tool choice instead of dropping it in buildBaseOptions.
+        toolChoice: options?.toolChoice,
+    };
     if (!options?.reasoning) {
         return streamGoogle(model, context, { ...base, thinking: { enabled: false } });
     }
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js b/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js
index fdc4439..8510672 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js
@@ -566,7 +566,7 @@ export function convertMessages(model, context, compat) {
                 });
             }
             else {
-                const content = msg.content.map((item) => {
+                const content = msg.content.filter((item) => item.type !== "file").map((item) => {
                     if (item.type === "text") {
                         return {
                             type: "text",
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses-shared.js b/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses-shared.js
index 3081455..183e6a5 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses-shared.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses-shared.js
@@ -79,7 +79,7 @@ export function convertResponsesMessages(model, context, allowedToolCallProvider
                 });
             }
             else {
-                const content = msg.content.map((item) => {
+                const content = msg.content.filter((item) => item.type !== "file").map((item) => {
                     if (item.type === "text") {
                         return {
                             type: "input_text",
diff --git a/node_modules/@mariozechner/pi-ai/dist/types.d.ts b/node_modules/@mariozechner/pi-ai/dist/types.d.ts
index df45596..0b1ae15 100644
--- a/node_modules/@mariozechner/pi-ai/dist/types.d.ts
+++ b/node_modules/@mariozechner/pi-ai/dist/types.d.ts
@@ -111,6 +111,16 @@ export interface ImageContent {
     data: string;
     mimeType: string;
 }
+export interface FileContent {
+    type: "file";
+    uri: string;
+    mimeType: string;
+    videoMetadata?: {
+        startOffset: string;
+        endOffset: string;
+        fps?: number;
+    };
+}
 export interface ToolCall {
     type: "toolCall";
     id: string;
@@ -135,7 +145,7 @@ export interface Usage {
 export type StopReason = "stop" | "length" | "toolUse" | "error" | "aborted";
 export interface UserMessage {
     role: "user";
-    content: string | (TextContent | ImageContent)[];
+    content: string | (TextContent | ImageContent | FileContent)[];
     timestamp: number;
 }
 export interface AssistantMessage {
@@ -154,7 +164,7 @@ export interface ToolResultMessage<TDetails = any> {
     role: "toolResult";
     toolCallId: string;
     toolName: string;
-    content: (TextContent | ImageContent)[];
+    content: (TextContent | ImageContent | FileContent)[];
     details?: TDetails;
     isError: boolean;
     timestamp: number;
