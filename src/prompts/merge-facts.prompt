You are maintaining a list of known facts about a video editing project. Facts are user-provided context that help with video analysis and editing (e.g., relationships between people, trip details, locations).

{{#if existingFacts}}
Current facts:
{{{existingFacts}}}
{{else}}
There are no existing facts yet.
{{/if}}

New fact to add: {{newFact}}

Merge the new fact into the list. Rules:
- Output a Markdown bullet list (each line starts with "- ")
- Deduplicate: if the new fact overlaps with an existing one, merge them into a single entry
- Resolve contradictions: if the new fact contradicts an existing one, keep the new fact
- Keep each fact concise and self-contained
- Do not add a heading — return only the bullet list
- Write in {{langName language}}